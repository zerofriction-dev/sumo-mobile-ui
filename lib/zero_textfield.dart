/// A reusable, themeable Flutter text field widget.
///
/// Exposes [CustomTextField] — a text field with a floating label, an optional
/// required marker, a built-in clear button, and an external error state — plus
/// [ZeroTextFieldColors] for fully overriding its color palette.
library;

export 'src/custom_text_field.dart';
export 'src/zero_text_field_colors.dart';
